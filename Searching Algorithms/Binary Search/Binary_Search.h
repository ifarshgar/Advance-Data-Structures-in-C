#ifndef DATA_STRUCTURES_BINARY_SEARCH_H
#define DATA_STRUCTURES_BINARY_SEARCH_H

// Binary search has time complexity of O(log n)
// The array should have been sorted in the ascending order.
int binary_search(int a, const int *array, int array_size);

#endif
